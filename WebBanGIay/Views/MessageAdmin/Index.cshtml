@model IEnumerable<WebBanGIay.Models.LIENHE>
@{
    ViewBag.Title = "Quản lý Tin nhắn";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int page = ViewBag.Page ?? 1;
    int totalPages = ViewBag.TotalPages ?? 1;
}

<style>
    .msg-unread {
        background-color: #f8f9fa;
        font-weight: 600;
    }
    .msg-unread td {
        color: var(--text-main);
    }
</style>

<div class="container-fluid p-4">
    <h3 class="mb-4 fw-bold">📬 Hộp thư đến</h3>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <div class="card card-premium shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="bg-light">
                    <tr>
                        <th class="p-3">Người gửi</th>
                        <th class="p-3">Email/SĐT</th>
                        <th class="p-3">Nội dung</th>
                        <th class="p-3">Thời gian</th>
                        <th class="p-3 text-center">Trạng thái</th>
                        <th class="p-3 text-end">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model == null || !Model.Any())
                    {
                        <tr><td colspan="6" class="p-4 text-center text-muted">Chưa có tin nhắn nào.</td></tr>
                    }
                    else
                    {
                        foreach (var item in Model)
                        {
                            bool isRead = item.TrangThai ?? false;
                            string rowClass = isRead ? "" : "msg-unread";
                            
                            <tr class="@rowClass">
                                <td class="p-3 align-middle">@item.HoTen</td>
                                <td class="p-3 align-middle">
                                    <div>@item.Email</div>
                                    <small class="text-muted">@item.SDT</small>
                                </td>
                                <td class="p-3 align-middle">
                                    @{
                                        string content = item.NoiDung;
                                        if (content.Length > 50) { content = content.Substring(0, 50) + "..."; }
                                    }
                                    @content
                                </td>
                                <td class="p-3 align-middle">@string.Format("{0:dd/MM/yyyy HH:mm}", item.NgayGui)</td>
                                <td class="p-3 align-middle text-center">
                                    @if(isRead) { <span class="badge bg-secondary">Đã xem</span> }
                                    else { <span class="badge bg-primary">Mới</span> }
                                </td>
                                <td class="p-3 align-middle text-end">
                                    <a href="@Url.Action("Details", new { id = item.MaLH })" class="btn btn-sm btn-outline-primary"><i class="bi bi-eye"></i></a>
                                    <form action="@Url.Action("Delete", new { id = item.MaLH })" method="post" class="d-inline" onsubmit="return confirm('Xóa tin nhắn này?');">
                                        <button type="submit" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
    
     <div class="mt-3">
        @for (int i = 1; i <= totalPages; i++)
        {
            <a href="?page=@i" class="btn btn-sm @(i == page ? "btn-primary" : "btn-outline-secondary")">@i</a>
        }
    </div>
</div>
