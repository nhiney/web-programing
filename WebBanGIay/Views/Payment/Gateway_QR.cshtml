@{
    ViewBag.Title = "Thanh toán QR Code";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string qrUrl = $"https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=2|99|{ViewBag.TxnId}|{ViewBag.TongTien}|0|0|0|";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-success text-white text-center py-3">
                    <h5 class="mb-0"><i class="fas fa-qrcode me-2"></i>Thanh Toán VietQR</h5>
                </div>
                <div class="card-body text-center p-5">
                    <p class="text-muted mb-4">Vui lòng mở App Ngân hàng và quét mã bên dưới</p>
                    
                    <div class="qr-wrapper mb-4 p-3 border rounded d-inline-block bg-white position-relative">
                        <img src="@qrUrl" alt="QR Code" class="img-fluid" width="250" />
                        <!-- Loading Scan Effect -->
                        <div class="scan-line"></div>
                    </div>

                    <h4 class="fw-bold text-danger mb-2">@string.Format("{0:N0}", ViewBag.TongTien) đ</h4>
                    <p class="text-muted small">Nội dung: <span class="fw-bold text-dark">@ViewBag.TxnId</span></p>

                    <div class="alert alert-warning mt-4 d-flex align-items-center justify-content-center">
                        <div class="spinner-border spinner-border-sm me-2" role="status"></div>
                        <span>Đang chờ thanh toán...</span>
                    </div>
                </div>
                <div class="card-footer bg-light p-3 text-center">
                    <button class="btn btn-outline-secondary btn-sm" onclick="history.back()">Hủy giao dịch</button>
                    <!-- Simulation Button for User -->
                    <a href="@Url.Action("CompleteOrder", new { method = "VietQR" })" class="btn btn-success btn-sm ms-2">
                        [Giả Lập] Đã chuyển khoản
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .qr-wrapper {
        position: relative;
        overflow: hidden;
    }
    .scan-line {
        position: absolute;
        width: 100%;
        height: 2px;
        background: #0d6efd;
        top: 0;
        left: 0;
        box-shadow: 0 0 4px #0d6efd;
        animation: scan 2s linear infinite;
        opacity: 0.6;
    }
    @@keyframes scan {
        0% { top: 0; }
        100% { top: 100%; }
    }
</style>

<script>
    // Simulate auto-check every 5s
    setTimeout(function() {
        // Only for demo, in real app we poll server
        // location.href = '@Url.Action("CompleteOrder", new { method = "VietQR" })'; 
    }, 15000); 
</script>
